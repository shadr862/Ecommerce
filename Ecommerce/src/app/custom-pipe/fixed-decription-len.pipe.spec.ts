import { FixedDecriptionLenPipe } from './fixed-decription-len.pipe';

describe('FixedDecriptionLenPipe', () => {
  it('create an instance', () => {
    const pipe = new FixedDecriptionLenPipe();
    expect(pipe).toBeTruthy();
  });
});
